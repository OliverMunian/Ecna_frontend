import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    value : []
}

export const interventions = createSlice({
    name : 'interventions',
    initialState,
    reducers : {
        
    }
})